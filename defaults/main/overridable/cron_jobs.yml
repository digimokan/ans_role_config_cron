# set of cron jobs to emplace in user (or root) crontabs
cfg_cron_jobs: []
#   - job_label: "ansible_run_main_bkup"
#     job_user: "root"
#     job_minute: "0"   # may omit
#     job_hour: "2"     # may omit
#     job_day: "*"      # may omit
#     job_month: "*"    # may omit
#     job_weekday: "*"  # may omit
#     job_cmd: "/usr/local/bin/bkup_script.sh"

